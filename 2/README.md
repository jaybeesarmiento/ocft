# Write a Terraform code to provision a Linux EC2 instance for web server in development environment, the VM spec, template is at your own choice.


```
terraform plan -out tfplan
terraform apply tfplan
```